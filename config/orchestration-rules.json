{
  "rules": [
    {
      "id": "custom_example",
      "question_terms": ["example", "question"],
      "required_any": [["required term", "accepted alias"]],
      "answer_any": ["expected answer term"],
      "aliases": {
        "expected answer term": ["accepted alias"]
      }
    }
  ]
}
